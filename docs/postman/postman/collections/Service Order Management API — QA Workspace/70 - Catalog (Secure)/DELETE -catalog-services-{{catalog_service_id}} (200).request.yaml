$kind: http-request
name: DELETE /catalog/services/{{catalog_service_id}} (200)
url: "{{base_url}}/catalog/services/{{catalog_service_id}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test('status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 3000
