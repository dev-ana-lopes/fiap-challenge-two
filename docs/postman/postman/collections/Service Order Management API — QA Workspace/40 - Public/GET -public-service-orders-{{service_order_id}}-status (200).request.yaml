$kind: http-request
name: GET /public/service-orders/{{service_order_id}}/status (200)
url: "{{base_url}}/public/service-orders/{{service_order_id}}/status"
method: GET
auth:
  type: noauth
scripts:
  - type: beforeRequest
    code: |-
      eval(pm.globals.get('qa_guard_required_uuid'));
      qaGuardRequiredUuid('service_order_id');
    language: text/javascript
  - type: afterResponse
    code: |-
      pm.test('status 200', () => pm.response.to.have.status(200));
      eval(pm.globals.get('qa_parse_json'));
      const json = qaParseJson();
      pm.test('has status', () => pm.expect(json).to.have.property('status'));
    language: text/javascript
order: 1000
