$kind: http-request
name: DELETE /customers/{{customer_id_customers_crud}} (200) [disabled by default]
url: "{{base_url}}/customers/{{customer_id_customers_crud}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test('status 200', () => pm.response.to.have.status(200));
    language: text/javascript
order: 6000
