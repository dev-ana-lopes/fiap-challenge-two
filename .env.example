DATABASE_URL=postgresql+asyncpg://service_order_user:service_order_password@postgres:5432/service_order_db
APP_BASE_URL=http://localhost:8000

SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_FROM_EMAIL=
SMTP_USE_TLS=false
SMTP_USE_AUTH=false

# Live Testmail tests only
# Override SMTP_HOST/SMTP_PORT with a real SMTP relay when enabling Testmail.
TESTMAIL_API_KEY=
TESTMAIL_NAMESPACE=
TESTMAIL_ENABLED=false
TESTMAIL_API_BASE_URL=https://api.testmail.app/api/json

APPROVAL_TOKEN_SECRET=change-me-use-a-different-secret-from-jwt-in-production
APPROVAL_TOKEN_TTL_MINUTES=60
JWT_SECRET=your-secret-key-here-change-in-production-use-long-random-string
JWT_ALGORITHM=HS256
JWT_EXPIRATION_MINUTES=60
